%h4 Top 5 most improved fantasy players from 2011 to 2012 for the players with at least 500 at-bats.
- unless @fantacy_palyers.blank?
  %table{class: "table table-striped table-bordered table-condensed"}
    %thead
      %tr
        %th Player Id
        %th Player Name
        %th Improved Fantacy points (2011-2012)
    %tbody
      - @fantacy_palyers.first(5).each do |key,value|
        - player = Player.where(player_id: key).first
        - if player
          %tr
            %td= player.player_id
            %td= player.name
            %td= value
